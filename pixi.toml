[workspace]
name = "my-docs"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "osx-arm64"]

[dependencies]
hugo = "==0.164.0"

[tasks]
serve = "hugo server --buildDrafts"
build = "hugo --minify"
